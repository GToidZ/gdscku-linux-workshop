1. 

```
mv isc/ ter/
mv ter/ et/
mv et/ stl/
mv stl/ fir/
cd fir/stl/et/ter/isc
pwd
```

2. 

`find pile-of-files -type f -not -empty`

3.

`head -n4 ../README.md | tail -n1 | wc -c`

4. Put `kasetsart` into answer file then execute `send-answer`

5.  

`tr 'n-za-mN-ZA-M' 'a-zA-Z'`

6. 

`tar -tf box.tar.gz | wc -l`

7. 

`sort letters.txt | uniq -u`

8.

`man tar`
Then, you'll see that `-A` is for concatenating two archives together so the answer is A.
